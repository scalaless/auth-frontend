import React from "react";
import StatteButton from "./StatteButton";

const StateWork = ({stateTitle}) => {
    return (
        <div>
            <h1>{stateTitle}</h1>
            <StatteButton/>            
        </div>

    )
}

export default StateWork